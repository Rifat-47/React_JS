import CreateForm from "./CreateForm";

const CreateData = () => {
    const createDataHandler = (title, body) => {
        
    };

    return (
        <CreateForm createDataHandler={createDataHandler} />
    );
};

export default CreateData;